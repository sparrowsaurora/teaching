# by now you can store a string in a variable
# this is similar
# however instead of placeing the word dierctly.
# we are going to use a function much like out print statement

user_input = input()

# thats it.

# this will ask for a string from the user
# and then we assign it to a variable

# we can then print

print("user entered: " + user_input)


# we can also ask the user a question within the input statement, so it isnt a blank question
# simply buy putting text instide the input() function
_user_input = input("please answer again: ")


# try running it in your own.
